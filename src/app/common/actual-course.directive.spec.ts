import { ActualCourseDirective } from './actual-course.directive';

describe('ActualCourseDirective', () => {
  it('should create an instance', () => {
    const directive = new ActualCourseDirective();
    expect(directive).toBeTruthy();
  });
});
